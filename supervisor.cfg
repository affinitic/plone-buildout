[supervisor]
recipe = collective.recipe.supervisor
port = ${hosts:supervisor}:${ports:supervisor}
serverurl = http://${hosts:supervisor}:${ports:supervisor}

[supervisord-initd]
recipe = collective.recipe.template
url = ${urls:buildout-repository}/templates/supervisord?login=bsuttor&token=9520b5a9dd1ddc6f0cfea557f76e32bc
output = ${buildout:directory}/etc/init.d/supervisord

[cmd-supervisor]
recipe = collective.recipe.cmd
on_install=true
cmds = 
    sed -i "s/tochange/`whoami`/g" ${supervisord-initd:output}
