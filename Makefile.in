BUILDOUT_VERSION ?= undefined

.PHONY: clean

clean: 
	rm -rf plone-buildout

Makefile: clean plone-buildout/Makefile
	cp plone-buildout/Makefile $@

plone-buildout/Makefile:
	git clone --depth=1 --branch=$(BUILDOUT_VERSION) git@github.com:CIRB/plone-buildout.git
